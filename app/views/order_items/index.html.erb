<h1>Listing order_items</h1>

<table>
  <thead>
    <tr>
      <th>Order</th>
      <th>Variant</th>
      <th>Shopify product</th>
      <th>Shopify variant</th>
      <th>Unit price</th>
      <th>Quantity</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @order_items.each do |order_item| %>
      <tr>
        <td><%= order_item.order_id %></td>
        <td><%= order_item.variant_id %></td>
        <td><%= order_item.shopify_product_id %></td>
        <td><%= order_item.shopify_variant_id %></td>
        <td><%= order_item.unit_price %></td>
        <td><%= order_item.quantity %></td>
        <td><%= link_to 'Show', order_item %></td>
        <td><%= link_to 'Edit', edit_order_item_path(order_item) %></td>
        <td><%= link_to 'Destroy', order_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order item', new_order_item_path %>
